文件传输

上传文件
客户端上传初始化
通过文件选择器获取要上传文件的路径，并作为成员变量存下来
当前路径、文件名、文件大小发送给服务器
服务端处理初始化
定义成员变量：文件对象、文件大小、文件已接收大小
取出当前路径和文件名，拼接得到完整路径，创建文件并更新成员变量文件对象
取出文件大小并更新成员变量
客户端开始上传文件内容给服务器
定义上传文件函数
打开要上传的文件
循环读取文件内容，将每一次读取的内容发送给服务器
ResHandler新建处理函数，取出上传初始化的结果，如果成功就调用上传文件函数
服务器接收文件内容写入文件，并更新已接收文件大小
将文件内容写入文件，更新已接收大小成员变量
判断已接收大小和文件总大小，如果不等直接返回继续接收上传
如果相等，将上传完成的结果响应给客户端
客户端刷新文件
二、代码部分 - 上传文件相关函数
1. void File::on_upload_PB_clicked()


void File::on_upload_PB_clicked()
{
    m_strUploadPath = QFileDialog::getOpenFileName();
    qDebug() << m_strUploadPath;
    int index = m_strUploadPath.lastIndexOf('/');
    QString strFileName = 
        m_strUploadPath.right(m_strUploadPath.size() - index - 1);
    QFileInfo fileInfo(m_strUploadPath);
    qint64 iFileSize = fileInfo.size();
    PDU *pdu = mkPDU(m_strUploadPath.toStdString().size() + 1);
    pdu->uiMsgType = ENUM_MSG_TYPE_UPLOAD_FILE_INIT_REQUEST;
    memcpy(pdu->caData, strFileName.toStdString().c_str(), 32);
    memcpy(pdu->caData+32, (char*)&iFileSize, sizeof(qint64));
    memcpy(pdu->caMsg, m_strUploadPath.toStdString().c_str(),
        m_strUploadPath.toStdString().size());
    Client::getInstance().sendMsg(pdu);
}


2. class MsgHandler : public QObject


class MsgHandler : public QObject
{
    // ...
private:
    QFile m_fUploadFile;
    qint64 m_iUploadTotal;
    qint64 m_iUploadReceived;
};
3. PDU *MsgHandler::uploadFileInit()


PDU *MsgHandler::uploadFileInit()
{
    char caFileName[32] = {'\0'};
    memcpy(caFileName, pdu->caData, 32);
    memcpy(&m_iUploadTotal, pdu->caData+32, sizeof(qint64));
    m_iUploadReceived = 0;
    QString strPath = 
        QString("%1/%2").arg(pdu->caMsg).arg(caFileName);
    m_fUploadFile.setFileName(strPath);
    bool ret = m_fUploadFile.open(QIODevice::WriteOnly);
    PDU* respdu = mkPDU(0);
    memcpy(respdu->caData, &ret, sizeof(bool));
    respdu->uiMsgType = ENUM_MSG_TYPE_UPLOAD_FILE_INIT_RESPOND;
    return respdu;
}
4. void File::uploadFile()


void File::uploadFile()
{
    QFile file(m_strUploadPath);
    if (!file.open(QIODevice::ReadOnly)) {
        QMessageBox::information(this, "提示", "上传文件失败");
        return;
    }
    while (true) {
        PDU* pdu = mkPDU(4096);
        pdu->uiMsgType = ENUM_MSG_TYPE_UPLOAD_FILE_DATA_REQUEST;
        qint64 ret = file.read(pdu->caMsg, 4096);
        if (ret < 0) {
            QMessageBox::information(this, "提示", "上传文件失败");
            return;
        }
        if (ret == 0) {
            break;
        }
        pdu->uiMsgLen = ret;
        pdu->uiTotalLen = ret + sizeof(PDU);
        Client::getInstance().sendMsg(pdu);
    }
}
5. void ResHandler::uploadFileInit()


void ResHandler::uploadFileInit()
{
    bool ret;
    memcpy(&ret, pdu->caData, sizeof(bool));
    if (ret) {
        Index::getInstance().getFile()->flushFile();
    } else {
        QMessageBox::information(&Index::getInstance(), "提示", "上传文件失败");
    }
}
6. PDU *MsgHandler::uploadFileData()


PDU *MsgHandler::uploadFileData()
{
    m_fUploadFile.write(pdu->caMsg, pdu->uiMsgLen);
    m_iUploadReceived += pdu->uiMsgLen;
    if (m_iUploadReceived < m_iUploadTotal) {
        return NULL;
    }
    PDU* respdu = mkPDU(0);
    respdu->uiMsgType = ENUM_MSG_TYPE_UPLOAD_FILE_DATA_RESPOND;
    return respdu;
}


7. 消息处理分支（case）


case ENUM_MSG_TYPE_UPLOAD_FILE_DATA_RESPOND: {
    Index::getInstance().getFile()->flushFile();
    break;
}
三、多线程上传部分
1. void File::uploadFile()（多线程版本）


void File::uploadFile()
{
    emit finished();
    return;
    
    while (true) {
        PDU* pdu = mkPDU(4096);
        pdu->uiMsgType = ENUM_MSG_TYPE_UPLOAD_FILE_DATA_REQUEST;
        qint64 ret = file.read(pdu->caMsg, 4096);
        if (ret < 0) {
            emit errorSignal("上传文件失败");
            break;
        }
        if (ret == 0) {
            break;
        }
        pdu->uiMsgLen = ret;
        pdu->uiTotalLen = ret + sizeof(PDU);
        emit uploadPDU(pdu);
    }
    file.close();
    emit finished();
}


2. Uploader::Uploader(QString strFilePath)


Uploader::Uploader(QString strFilePath)
{
    m_strUploadPath = strFilePath;
}


3. void Uploader::start()


void Uploader::start()
{
    QThread* thread = new QThread;
    this->moveToThread(thread);
    connect(thread, &QThread::started, this, 
        &Uploader::uploadFile, Qt::QueuedConnection);
    connect(this, &Uploader::finished, thread, &QThread::quit,
        Qt::QueuedConnection);
    connect(thread, &QThread::finished, thread, 
        &QThread::deleteLater, Qt::QueuedConnection);
    thread->start();
}


4. void File::uploadFile()（创建Uploader版本）


void File::uploadFile()
{
    Uploader* uploader = new Uploader(m_strUploadPath);
    connect(uploader, &Uploader::errorSignal, this, 
        &File::errorBox, Qt::QueuedConnection);
    connect(uploader, &Uploader::uploadPDU,
        &Client::getInstance(), &Client::sendMsg, 
        Qt::QueuedConnection);
    uploader->start();
}


四、文件分享部分
分享文件流程说明
判断是否选择了要分享的文件（当前项）
文件新增一个成员变量保存分享的文件名
判断分享文件界面是否隐藏，如果隐藏显示出来
显示之前需要填充分享界面中的好友列表
ShareFile类中增加一个填充好友列表的函数
填充数据从 Friend类中的列表框中获取（Friend类中增加一个获取好友列表框的公有函数）
遍历 Friend类中的列表框，复制元素给 ShareFile类的列表框
分享确定按钮槽函数发送请求
拼接当前路径和分享文件名为完整路径
选择的好友名和路径放入 caMsg，当前用户名和选中的好友数量放入 caData
发送请求给服务器
服务器转发给分享用户并响应
取出当前用户名和好友数量
遍历好友（caMsg），进行转发，转发送者的名字和文件路径
服务器响应给发送者
发送者客户端弹窗显示 文件已分享
转发好友客户端处理是否接收
从路径中取出文件名，弹窗询问用户是否接收
如果接收，将当前用户名和文件路径发给服务器
服务器处理同意分享文件
得到接收路径（接收用户的根目录）和分享文件路径
通过分享文件路径得到分享文件名，文件名与接收路径拼接成完整路径
复制文件
结果发送给客户端
客户端显示结果


五、分享文件相关函数
1. void ShareFile::on_allSelect_PB_clicked()


void ShareFile::on_allSelect_PB_clicked()
{
    for (int i=0; i<ui->listWidget->count(); i++) {
        ui->listWidget->item(i)->setSelected(true);
    }
}


2. void ShareFile::on_cancelSelect_PB_clicked()


void ShareFile::on_cancelSelect_PB_clicked()
{
    for (int i=0; i<ui->listWidget->count(); i++) {
        ui->listWidget->item(i)->setSelected(false);
    }
}


3. QListWidget* Friend::getFriend_LW()


QListWidget* Friend::getFriend_LW()
{
    return ui->listWidget;
}


4. void ShareFile::updateLW()


void ShareFile::updateLW()
{
    ui->listWidget->clear();
    QListWidget* friend_LW = 
        Index::getInstance().getFriend()->getFriend_LW();
    for (int i=0; i<friend_LW->count(); i++) {
        QListWidgetItem* friendItem = friend_LW->item(i);
        QListWidgetItem* newItem = new 
            QListWidgetItem(*friendItem);
        ui->listWidget->addItem(newItem);
    }
}


5. void File::on_share_PB_clicked()


void File::on_share_PB_clicked()
{
    // 判断是否选择了文件
    QListWidgetItem* pItem = ui->listWidget->currentItem();
    if (!pItem) {
        return;
    }
    m_pShareFile->m_strFileName = pItem->text();
    m_pShareFile->updateLW();
    if (m_pShareFile->isHidden()) {
        m_pShareFile->show();
    }
}


6. void ShareFile::on_ok_PB_clicked()


void ShareFile::on_ok_PB_clicked()
{
    QString strCurName = Client::getInstance().m_strLoginName;
    QString strCurPath = 
        Index::getInstance().getFile()->m_strCurPath;
    QString strSharePath = 
        QString("%1/%2").arg(strCurPath).arg(m_strFileName);
    QList<QListWidgetItem*> pItems = 
        ui->listWidget->selectedItems();
    int iFriendSize = pItems.size();
    PDU* pdu = mkPDU(32*iFriendSize+strSharePath.toStdString().size()+1);
    pdu->uiMsgType = ENUM_MSG_TYPE_SHARE_FILE_REQUEST;
    memcpy(pdu->caData, strCurName.toStdString().c_str(), 32);
    memcpy(pdu->caData+32, &iFriendSize, sizeof(int));
    for (int i=0; i<iFriendSize; i++) {
        memcpy(pdu->caMsg+i*32, 
            pItems[i]->text().toStdString().c_str(), 32);
    }
    memcpy(pdu->caMsg+32*iFriendSize, 
        strSharePath.toStdString().c_str(),
        strSharePath.toStdString().size());
    Client::getInstance().sendMsg(pdu);
}


7. PDU *MsgHandler::shareFile()


PDU *MsgHandler::shareFile()
{
    char strCurName[32] = {'\0'};
    int iFriendSize = 0;
    memcpy(strCurName, pdu->caData, 32);
    memcpy(&iFriendSize, pdu->caData+32, sizeof(int));
    PDU* resendpdu = mkPDU(pdu->uiMsgLen-iFriendSize*32);
    resendpdu->uiMsgType = pdu->uiMsgType;
    memcpy(resendpdu->caData, strCurName, 32);
    memcpy(resendpdu->caMsg, pdu->caMsg+iFriendSize*32,
        pdu->uiMsgLen-iFriendSize*32);
    char caTmp[32] = {'\0'};
    for (int i=0; i<iFriendSize; i++) {
        memcpy(caTmp, pdu->caMsg+i*32, 32);
        MyTcpServer::getInstance().resend(caTmp, resendpdu);
    }
    free(resendpdu);
    resendpdu = NULL;
    
    PDU* respdu = mkPDU(0);
    respdu->uiMsgType = ENUM_MSG_TYPE_SHARE_FILE_RESPOND;
    return respdu;
}


8. void ResHandler::shareFileResend()


void ResHandler::shareFileResend()
{
    QString strSharePath = QString(pdu->caMsg);
    int index = strSharePath.lastIndexOf('/');
    QString strFileName = strSharePath.right(strSharePath.size() - 
        index - 1);
    QString strMsg = QString("%1 分享文件: %2\n是否接收?")
        .arg(pdu->caData).arg(strFileName);
    int ret = QMessageBox::question(Index::getInstance(), "分享文件",
        strMsg);
    if (ret != QMessageBox::Yes) {
        return;
    }
    PDU* respdu = mkPDU(pdu->uiMsgLen);
    respdu->uiMsgType = ENUM_MSG_TYPE_SHARE_FILE_AGREE_REQUEST;
    memcpy(respdu->caData, 
        Client::getInstance().m_strLoginName.toStdString().c_str(), 32);
    memcpy(respdu->caMsg, pdu->caMsg, pdu->uiMsgLen);
    Client::getInstance().sendMsg(respdu);
}


9. PDU *MsgHandler::shareFileAgree()


PDU *MsgHandler::shareFileAgree()
{
    QString strShareFilePath = pdu->caMsg;
    int index = strShareFilePath.lastIndexOf('/');
    QString strFileName = strShareFilePath.right(
        strShareFilePath.size() - index - 1);
    QString strTarPath = QString("%1/%2")
        .arg(Server::getInstance().m_strRootPath).arg(pdu->caData)
        .arg(strFileName);
    bool ret = QFile::copy(strShareFilePath, strTarPath);
    PDU* respdu = mkPDU(0);
    respdu->uiMsgType = ENUM_MSG_TYPE_SHARE_FILE_AGREE_RESPOND;
    memcpy(respdu->caData, &ret, sizeof(bool));
    return respdu;
}


10. void ResHandler::shareFileAgree()


void ResHandler::shareFileAgree()
{
    bool ret;
    memcpy(&ret, pdu->caData, sizeof(bool));
    if (ret) {
        QMessageBox::information(&Index::getInstance(), "提示", "分享文件成功");
    } else {
        QMessageBox::information(&Index::getInstance(), "提示", "分享文件失败");
    }
}


六、函数汇总列表
表格
序号	函数名	所属类	功能
1	on_upload_PB_clicked()	File	上传按钮点击槽函数，初始化上传
2	uploadFileInit()	MsgHandler	服务器端处理上传初始化
3	uploadFile()	File	客户端读取并发送文件数据
4	uploadFileInit()	ResHandler	客户端处理上传初始化响应
5	uploadFileData()	MsgHandler	服务器端接收并写入文件数据
6	uploadFile()	File（多线程）	多线程版本文件上传
7	Uploader()	Uploader	上传器构造函数
8	start()	Uploader	启动上传线程
9	uploadFile()	File（创建Uploader）	创建上传器并启动
10	on_allSelect_PB_clicked()	ShareFile	全选好友按钮
11	on_cancelSelect_PB_clicked()	ShareFile	取消全选按钮
12	getFriend_LW()	Friend	获取好友列表控件
13	updateLW()	ShareFile	更新分享文件好友列表
14	on_share_PB_clicked()	File	分享按钮点击槽函数
15	on_ok_PB_clicked()	ShareFile	确认分享按钮
16	shareFile()	MsgHandler	服务器端转发分享请求
17	shareFileResend()	ResHandler	客户端处理被分享文件
18	shareFileAgree()	MsgHandler	服务器端处理同意分享
19	shareFileAgree()	ResHandler	客户端处理分享结果
